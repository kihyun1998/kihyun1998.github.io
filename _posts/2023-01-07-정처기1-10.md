---
title : "[정보처리기사] 1과목 1장 요구사항 확인 - 주요 UML 다이어그램"
date : 2023-01-07 01:30:00 +0900
categories : [study, 정보처리기사]
tags: [자격증,정보처리기사,요구사항 확인, A등급]
---

# **Section 10.** 주요 UML 다이어그램
---

## 유스케이스 다이어그램

사용자와 다른 외부 시스템들이 개발될 시스템을 이용해 수행할 수 있는 기능을 사용자의 관점에서 표현한 것

- 외부 요소와 시스템 간의 상호 작용 확인 가능
- 사용자의 요구사항 분석 도구로 사용됨
- 시스템의 범위 파악 가능

### 구성요소

- 시스템/시스템 범위( System Scope )
    - 시스템 내부에서 수행되는 기능들을 외부 시스템과 구분하기 위해 내부 유스케이스들을 사각형으로 묶어 시스템 범위를 표현
- 액터( Actor )
    - 시스템과 상호작용을 하는 모든 외부 요소
    - 사람이나 외부 시스템
    > `주액터` : 시스템을 사용함으로 이득을 얻는 대상 ( 사람 ), 사용자 액터라고도 함.
    > `부액터` : 주액터의 목적 달성을 위해 서비스를 제공하는 외부 시스템 ( 조직, 기관 ) 시스템 액터라고도 함.
- 유스케이스( Use Case )
    - 사용자가 보는 관점에서 시스템이 액터에게 제공하는 서비스 또는 기능을 표현한 것 
- 관계( Relationship )
    - 포함 관계 ( Include ): 
        - 유스케이스에 공통적으로 적용되는 기능을 별도로 분리해서 새 유스케이스를 만듬
        - 기존 유스케이스와 새 유스케이스 관계
        - 점선 화살표와 `<<include>>`로 표기
    - 확장 관계 ( Extend ):
        - 유스케이스가 특정 조건에 부합되어 유스케이스의 기능이 확장될 때
        - 기존 유스케이스와 확장된 유스케이스의 관계
        - 점선 화살표와 `<<extends>>`로 표기



## 클래스 다이어그램

클래스와 속성, 오퍼레이션, 제약조건, 사이의 관계를 표현한 것

- 구조적 다이어그램이다.
- 시스템 구성 요소를 문서화하는데 사용
- 객체 속성, 함수 등을 잘 표현하여 시스템을 모델링 하는데 자주 사용됨

### 구성요소

- 클래스 ( Class )
    - 객체들이 갖는 속성과 오퍼레이션(동작)을 표현
    - 3개의 구획( Compartment )으로 나눠 클래스의 이름, 속성, 오퍼레이션을 표기
    > `속성( Attribute )` : 클래스의 상태나 정보를 표현
    > `오퍼레이션( Operation ) ` : 클래스가 수행할 수 있는 동작, 함수라고도 함.
- 제약조건
    - 입력 값에 대한 제약조건이나 오퍼레이션 수행 전후에 지정해야할 조건이 있다면 작성
- 관계 ( Relationships )
    - 클래스와 클래스 사이의 연관성을 표현
    - 종류로는 연관 관계, 집합 관계, 포함 관계, 일반화 관계, 의존 관계가 있다.



## 순차 다이어그램 ( Sequence )

시스템이나 객체들이 메시지를 주고 받으며 시간의 흐름에 따라 상호 작용하는 과정을 액터, 객체, 메시지 등의 요소를 사용하여 그림으로 표현

- 상호 작용 과정에서 주고받는 메시지를 표현
- 동작에 참여하는 시스템이나 객체들의 수행 기간 확인
- 객체들을 기본단위로 하여 상호작용을 표현
- 주로 기능 모델링에서 작성한 유스케이스 명세서를 하나의 표현 범위로 함
- 클래스에 포함된 오퍼레이션을 하나의 범위로 하기도 함.

### 구성요소

- 액터( Actor ) : 시스템으로부터 서비스를 요청하는 외부 요소, ( 사람이나 외부 시스템 )
- 객체( Object ) : 메시지를 주고받는 객체
- 생명선( Lifeline ) : 객체가 메모리에 존재하는 기간, 객체 아래쪽에 점선을 그어 표현
- 실행 상자( Active Box ) : 객체가 메시지를 주고 받으며 구동되고 있음을 표현
- 메시지( Message ) : 객체가 상호 작용을 위해 주고받는 메시지